[project]
name = "fantasy-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "nfl_data_py>=0.3",
    "pulp>=2.8",
    "langgraph>=1.2.11",
    "langchain-openai>=1.6.2",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
